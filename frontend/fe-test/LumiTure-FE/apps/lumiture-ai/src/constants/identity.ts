export enum Depth {
  ADMIN = 0,
  T1 = 1,
  T2 = 2,
}

export enum OAuthProviderEnum {
  Google = 'google',
  AzureAd = 'microsoft',
  GitHub = 'github',
}

export enum Role {
  OWNER = 'owner',
  ADMIN = 'admin',
  MANAGER = 'manager',
  MEMBER = 'member',
}
